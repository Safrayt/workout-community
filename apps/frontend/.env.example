# Адрес бэкенда (FastAPI). По умолчанию, если переменная не задана,
# используется http://127.0.0.1:8000 — см. src/api/config.ts.
#
# Скопируйте этот файл в .env (или .env.local) и поправьте адрес,
# если бэкенд запущен не локально или не на стандартном порту.
VITE_API_URL=http://127.0.0.1:8000
