/**
 * Стартовый вид карты для форм выбора точки на карте (создание
 * площадки/мероприятия/записи дневника с привязкой к площадке) —
 * в отличие от информационных карт списка площадок/мероприятий/
 * ленты, здесь сразу нужен вид на уровне города: пользователь
 * выбирает конкретную точку, а не разглядывает карту материков.
 *
 * Минск — основной город сообщества портала на данный момент.
 */
export const DEFAULT_MAP_PICKER_CENTER: [number, number] = [53.9, 27.5667];
export const DEFAULT_MAP_PICKER_ZOOM = 12;
