/** Ограничение поля "О себе" (UX-PROFILE §16). */
export const MAX_ABOUT_LENGTH = 300;

/** Сколько последних записей дневника/достижений/событий показывать в превью на профиле (UX-PROFILE §20, §22, §24). */
export const PROFILE_PREVIEW_LIMIT = 3;

/** Небольшой список в разделе "Подписки" — не полноценная страница со всеми подписками. */
export const SUBSCRIPTIONS_PREVIEW_LIMIT = 8;
